<nav>
    <h2>Subcategorias</h2>
    <p><a href="/">Voltar</a></p>
    <p><a href="/subcategories/create">Cadastar</a></p>
</nav>
<ul class="list">
    {{#if emptySubcategories}}
        <p>Não há subcategorias cadastrados</p>
    {{/if}}
    {{#each subcategories}}
        <li>
            {{this.name}}
        </li>
        <span class="actions">
            <a href="/subcategories/edit/{{this.id}}"><i class="bi bi-pencil-square"></i></a> 
            <form action="/subcategories/remove" method="post">
                <input type="hidden" name="id" value="{{this.id}}">
                <button type="submit">
                    <i class="bi bi-x-lg"></i>
                </button>
            </form>
        </span>
    {{/each}}
</ul>