<nav>
    <h2>Auto Peças</h2>
    <p><a href="/">Voltar</a></p>
    <p><a href="/autoparts/create">Cadastar</a></p>
</nav>
<ul class="list">
    {{#if emptyAutoparts}}
        <p>Não há auto peças cadastradas</p>
    {{/if}}
    {{#each autoparts}}
        <li>
            {{this.name}}
        </li>
        <span class="actions">
            <a href="/autoparts/edit/{{this.id}}"><i class="bi bi-pencil-square"></i></a> 
            <form action="/autoparts/remove" method="post">
                <input type="hidden" name="id" value="{{this.id}}">
                <button type="submit">
                    <i class="bi bi-x-lg"></i>
                </button>
            </form>
        </span>
    {{/each}}
</ul>